signal-ingestion:
  image:
    tag: dev

data-storage:
  image:
    tag: dev

scoring-engine:
  image:
    tag: dev

ai-mockup-generation:
  image:
    tag: dev

marketplace-publisher:
  image:
    tag: dev

feedback-loop:
  image:
    tag: dev

orchestrator:
  image:
    tag: dev

backup-jobs:
  image:
    tag: latest

logrotate-jobs:
  image:
    tag: latest

# Grafana configuration
grafana:
  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      searchNamespace: ALL
