apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "signal-ingestion.fullname" . }}
  labels: {{- include "signal-ingestion.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: {{- include "signal-ingestion.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "signal-ingestion.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
