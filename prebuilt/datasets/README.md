Place dataset archives used for tests or local experimentation here.
The default `DATASET_DIR` environment variable points to this directory so
services can load datasets without additional network access.
